# 游玩前请阅读此文档,游戏特色：不要红温
## 作者：`深藏blue`全体成员

若难度过大,请查看代码Selcet_Plant_View.cpp中第185行，按照需求修改即可

`else if (currentSelectedPlant == 8 && (sp == true /* || sp == false */))`

## 引用库
* easyX
* irrKlang
* opencv

* 开始游戏前先检查项目->属性->配置属性->调试->环境变量中是否有东西

* 若有，则正常运行即可

* 若无，请添加这样一行`PATH=bin;%PATH%`

* 该库已做整合，直接运行即可

* 注册若出现bug，找到`./src/data/user.dat`并删除`user.dat`

## 游玩前注意事项

* 一定要阅读游戏目录中的 `玩法介绍`
* 一定要仔细阅读第四页
* 有技能，可能是 `被动技能` 可能是 `主动技能`
* `主动技能` 按 `f` 启动
* `被动技能` 自动释放
* **左上角** 有 `生命值` 和 `护盾值` 及 `技能冷却时间`
* **右上角** 是 `暂停`
* **中间** 是 `进度条`

## 不要红温，~~不可以骂开发者~~，轻点就行了~
不可以骂开发者

~~不~~可以骂开发者

好吧，可以

### 大佬攻略


- `第四关` 打不过试试 ~~建国~~ (`坚果`
- `第三关` 打不过试试 `卷心菜`
- `第二关` 打不过试试 `冰瓜炮`
- `第一关` 打~不~得过的
- 打不过往 `右` 走
- 还打不过用 `蜗牛`
- 用 `蜗牛` 打得过的